return {
  'm4xshen/smartcolumn.nvim',
  event = 'VeryLazy',

  opts = {
    colorcolumn = 80,
    disabled_filetypes = { 'help', 'text', 'markdown', 'lazy' },
    custom_colorcolumn = {},
    limit_to_window = false,
    limit_to_line = true,
  }
}
